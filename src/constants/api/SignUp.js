import * as Constants from '../index';

export const SAVE_REGISTER = Constants.API + '/saveregister';

export const USER_LOGIN = Constants.API + '/login';

export const PHONE_CODE = Constants.API + '/phonecode';

